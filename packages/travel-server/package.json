{
  "name": "travel-server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && yarn esbuild src/index.ts --bundle --minify --platform=node --outfile=dist/index.js",
    "build:dev": "rm -rf dist && yarn tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "app-db": "workspace:^",
    "fastify": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.1",
    "esbuild": "^0.24.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "packageManager": "yarn@4.5.0"
}
